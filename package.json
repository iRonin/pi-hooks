{
  "name": "@ironin/pi-hooks",
  "version": "1.0.0",
  "description": "Flexible directory-based permission system for Pi Agent bash commands. Control which commands require approval, are blocked, or allowed via .pi-hooks config files.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "pi-agent",
    "extension",
    "permissions",
    "security",
    "bash",
    "hooks"
  ],
  "author": "iRonin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/iRonin/pi-hooks.git"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.1.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
